[project]
name = "rubicon-openenv"
version = "0.1.0"
dependencies = ["openenv-core"]

[project.scripts]
server = "server.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"